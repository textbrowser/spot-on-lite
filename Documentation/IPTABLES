# Delete rule.

sudo iptables -D INPUT 1

# Limit 2 connections per IP.

iptables -A INPUT \
	 -p tcp \
	 --dport 4710 \
	 --syn \
	 -m connlimit \
	 --connlimit-above 2 \
	 --connlimit-mask 32 \
	 -j REJECT \
	 --reject-with tcp-reset

# List contents.

sudo iptables -L --list-numbers
