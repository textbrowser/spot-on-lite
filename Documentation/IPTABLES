# Delete rule.

sudo iptables -D INPUT 1

# Limit 2 connections per IP.

iptables -A INPUT \
	 -p tcp \
	 --dport 4710 \
	 --syn \
	 -j REJECT \
	 --reject-with tcp-reset \
	 -m connlimit \
	 --connlimit-above 2 \
	 --connlimit-mask 32

# List contents.

sudo iptables -L --line-numbers
